# name: Test Azure OIDC Login
# on:
#   push:
#     branches: [ "main" ]

# permissions:
#   id-token: write
#   contents: read

# jobs:
#   test-login:
#     runs-on: ubuntu-latest
#     steps:
#       - name: Debug presence (no secret values printed)
#         run: |
#           echo "vars.CLIENT_ID present? ${{ vars.AZURE_CLIENT_ID != '' }}"
#           echo "secrets.CLIENT_ID present? ${{ secrets.AZURE_CLIENT_ID != '' }}"
#           echo "vars.TENANT_ID present? ${{ vars.AZURE_TENANT_ID != '' }}"
#           echo "vars.SUBSCRIPTION present? ${{ vars.AZURE_SUBSCRIPTION_ID != '' }}"

#       - name: Azure login (OIDC)
#         uses: azure/login@v2
#         with:
#           client-id: ${{ vars.AZURE_CLIENT_ID || secrets.AZURE_CLIENT_ID }}
#           tenant-id: ${{ vars.AZURE_TENANT_ID || secrets.AZURE_TENANT_ID }}
#           subscription-id: ${{ vars.AZURE_SUBSCRIPTION_ID || secrets.AZURE_SUBSCRIPTION_ID }}

#       - name: Verify account
#         run: |
#           az account show -o table
#           az account show --query "{tenant:tenantId, sub:id}" -o table
